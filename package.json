{
  "name": "local-dev-manager",
  "version": "1.3.6",
  "description": "A desktop app to manage and launch your local dev projects",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-rebuild -f -w node-pty && electron-builder --mac",
    "build:all": "electron-rebuild -f -w node-pty && electron-builder --mac --win --linux",
    "test": "vitest run",
    "test:watch": "vitest",
    "release": "bash scripts/release.sh",
    "release:minor": "bash scripts/release.sh minor",
    "release:major": "bash scripts/release.sh major"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-search": "^0.15.0",
    "@xterm/addon-unicode11": "^0.8.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "electron-rebuild": "^3.2.9",
    "vitest": "^4.1.2"
  },
  "build": {
    "appId": "com.local-dev-manager",
    "productName": "Local Dev Manager",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "renderer/**",
      "xterm-loader.js",
      "index.html",
      "styles.css",
      "package.json",
      "src/**",
      "node_modules/@xterm/**",
      "node_modules/node-pty/**"
    ],
    "mac": {
      "target": "dmg",
      "icon": "icon.icns",
      "category": "public.app-category.developer-tools"
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
